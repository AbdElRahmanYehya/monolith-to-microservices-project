apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-api-feed
spec:
  replicas: 2
  selector:
    matchLabels:
      app: udagram-api-feed
  template:
    metadata:
      labels:
        app: udagram-api-feed
    spec:
      containers:
        - name: api-feed
          image: aaboelella/udagram-api-feed:v1
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: env-config
            - secretRef:
                name: env-secret
            - secretRef:
                name: aws-secret
---
apiVersion: v1
kind: Service
metadata:
  name: backend-feed
spec:
  type: ClusterIP
  selector:
    app: udagram-api-feed
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
